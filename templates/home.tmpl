{{block "home-page" .}}
  {{template "head" .}}
  {{template "nav-head" .}}

  {{template "home" .}}

  {{template "nav-foot" .}}
  {{template "foot" .}}
{{end}}

{{block "home" .}}
  <h1 class="text-xl">Home</h1>
{{end}}
