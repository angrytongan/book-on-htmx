{{block "tabs-page" .}}
  {{template "head" .}}
  {{template "nav-head" .}}

  {{template "tabs"}}

  {{template "nav-foot" .}}
  {{template "foot" .}}
{{end}}

{{block "tabs" .}}
  <h1 class="text-xl">Tabs</h1>
{{end}}
